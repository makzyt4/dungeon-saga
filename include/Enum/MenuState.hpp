#ifndef DS_ENUM_MENUSTATE_HPP
#define DS_ENUM_MENUSTATE_HPP

namespace ds {
    enum class MenuState {
        Normal,
        Highlighted,
        Clicked,
        Checked,
        Unchecked
    };
}

#endif // DS_ENUM_MENUSTATE_HPP
